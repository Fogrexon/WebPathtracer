#include <vec3.h>
#include <ray.h>

#define MAX_LOOP 100


namespace raytracer {
  Color raytrace(Ray &ray, Material &material) {
    Vec3 pos = ray.origin;

    
    for(int i=0;i<100;i++) {

    }
  }
}